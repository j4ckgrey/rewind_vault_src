{
  "name": "@rewind/vault",
  "version": "1.0.0",
  "description": "The Vault — password-gated adult catalog/search/scrapers for Rewind, packaged as an operator-installed addon. Resolves streams through The Forge.",
  "license": "UNLICENSED",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "node build.mjs"
  },
  "dependencies": {
    "@rewind/addon-sdk": "file:../rewind_addon_sdk",
    "@rewind/forge": "file:../rewind_forge"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
